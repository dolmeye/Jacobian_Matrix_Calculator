# Jacobian_Matrix_Calculator
JacobianMatrixCalc is a MATLAB function that computes the **symbolic Jacobian matrix** of a system of equations. It also supports numerical evaluation at a given point. The function automatically extracts symbolic variables, performs symbolic differentiation, and optionally substitutes a specific point to return a numerical result.
